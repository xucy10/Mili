From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: xucy <xucy0609@gmail.com>
Date: Wed, 12 Aug 2026 12:41:55 +0800
Subject: [PATCH] Fix compilation for MC 26.2 API migration


diff --git a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
index e8c02d10901514da1fb2019218a7cc2de81e3487..a1fe6b35fe28384834620addb29919a76a6805a9 100644
--- a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
@@ -116,6 +116,7 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
             this.keepAlive.pingCalculator5s.update(response);
 
             this.latency = this.keepAlive.pingCalculator5s.getAvgLatencyMS();
+            this.lastKeepAliveResponse = now; // Mili - async keepalive
             return;
         }
 
@@ -283,6 +284,29 @@ public abstract class ServerCommonPacketListenerImpl implements ServerCommonPack
         Profiler.get().pop();
     }
 
+    // Mili start - async keepalive
+    public void keepConnectionAliveAsync(long currentTimeNs, long currentTimeMs) {
+        if (this.closed || !this.connection.isConnected()) {
+            return;
+        }
+
+        long timeoutNanos = java.util.concurrent.TimeUnit.MILLISECONDS.toNanos(fun.bm.mili.config.modules.function.OldFeatureConfig.asyncKeepaliveTimeoutSeconds * 1000L);
+        if ((currentTimeNs - this.keepAlive.lastKeepAliveTx) >= java.util.concurrent.TimeUnit.SECONDS.toNanos(1L)) {
+            this.keepAlive.lastKeepAliveTx = currentTimeNs;
+            io.papermc.paper.util.KeepAlive.PendingKeepAlive pending = new io.papermc.paper.util.KeepAlive.PendingKeepAlive(currentTimeNs, currentTimeMs);
+            this.keepAlive.pendingKeepAlives.add(pending);
+            this.send(new ClientboundKeepAlivePacket(pending.challengeId()));
+        }
+        while (this.keepAlive.pendingKeepAlives.pollIf((pending) -> (currentTimeNs - pending.txTimeNS()) > timeoutNanos) != null) {
+        }
+
+        if ((currentTimeNs - this.lastKeepAliveResponse) > timeoutNanos) {
+            LOGGER.info("{} was kicked due to keepalive timeout!", this.playerProfile().name());
+            this.disconnectAsync(TIMEOUT_DISCONNECTION_MESSAGE, io.papermc.paper.connection.DisconnectionReason.TIMEOUT);
+        }
+    }
+    // Mili end - async keepalive
+
     private boolean checkIfClosed(final long now) {
         if (this.closed) {
             if (now - this.closedListenerTime >= 15000L) {
diff --git a/net/minecraft/util/debug/ServerDebugSubscribers.java b/net/minecraft/util/debug/ServerDebugSubscribers.java
index 19caac6d353ed46962134a4e271e0e869e2844eb..97ac74f117547af8d5b6e583d2ba680ae9c9f136 100644
--- a/net/minecraft/util/debug/ServerDebugSubscribers.java
+++ b/net/minecraft/util/debug/ServerDebugSubscribers.java
@@ -13,7 +13,7 @@ import net.minecraft.server.players.NameAndId;
 
 public class ServerDebugSubscribers {
     private final MinecraftServer server;
-    private final Map<DebugSubscription<?>, List<ServerPlayer>> enabledSubscriptions = new HashMap<>();
+    private final Map<DebugSubscription<?>, List<ServerPlayer>> enabledSubscriptions = new ConcurrentHashMap<>(); // Mili fix - #472: thread-safe map
 
     public ServerDebugSubscribers(final MinecraftServer server) {
         this.server = server;
@@ -28,11 +28,11 @@ public class ServerDebugSubscribers {
 
         for (ServerPlayer player : this.server.getPlayerList().getPlayers()) {
             for (DebugSubscription<?> subscription : player.debugSubscriptions()) {
-                this.enabledSubscriptions.computeIfAbsent(subscription, s -> new ArrayList<>()).add(player);
+                this.enabledSubscriptions.computeIfAbsent(subscription, s -> new CopyOnWriteArrayList<>()).add(player);
             }
         }
 
-        this.enabledSubscriptions.values().removeIf(List::isEmpty);
+        this.enabledSubscriptions.entrySet().removeIf(entry -> entry.getValue().isEmpty()); // Mili fix - #472: safe removal
     }
 
     public void broadcastToAll(final DebugSubscription<?> subscription, final Packet<?> packet) {
diff --git a/net/minecraft/world/entity/Entity.java b/net/minecraft/world/entity/Entity.java
index 171c929677bfb0906a811ad9346c02a87204758f..aaed0f7a8f3b78e9e2c50014f2cfad536af3adb1 100644
--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -356,6 +356,7 @@ public abstract class Entity
     private final LongSet visitedBlocks = new LongOpenHashSet();
     private final InsideBlockEffectApplier.StepBasedCollector insideEffectCollector = new InsideBlockEffectApplier.StepBasedCollector();
     private CustomData customData = CustomData.EMPTY;
+    private net.minecraft.nbt.CompoundTag leavesData = new net.minecraft.nbt.CompoundTag(); // Leaves - Leaves ex data
     // CraftBukkit start
     public boolean persist = true;
     public boolean visibleByDefault = true;
@@ -2788,6 +2789,8 @@ public abstract class Entity
                 output.store("data", CustomData.CODEC, this.customData);
             }
 
+            output.store("Leaves.Data", net.minecraft.nbt.CompoundTag.CODEC, leavesData); // Leaves - leaves ex data
+
             this.addAdditionalSaveData(output, includeAll); // CraftBukkit - pass on includeAll
             if (this.isVehicle()) {
                 ValueOutput.ValueOutputList passengersList = output.childrenList("Passengers");
@@ -2892,6 +2895,7 @@ public abstract class Entity
                 );
                 // Paper end
                 this.customData = input.read("data", CustomData.CODEC).orElse(CustomData.EMPTY);
+                leavesData = input.read("Leaves.Data", net.minecraft.nbt.CompoundTag.CODEC).orElse(new net.minecraft.nbt.CompoundTag()); // Leaves - leaves ex data
                 this.tags.clear();
                 input.read("Tags", TAG_LIST_CODEC).ifPresent(this.tags::addAll);
                 this.readAdditionalSaveData(input);
@@ -6600,4 +6604,10 @@ public abstract class Entity
     }
     // Mili end - entity culling
     public boolean shouldTickHot() { return this.tickCount > 20 * 10 && this.isAlive(); } // KioCG
+
+    // Leaves start - leaves ex data
+    public net.minecraft.nbt.CompoundTag getLeavesData() {
+        return leavesData;
+    }
+    // Leaves end - leaves ex data
 }
diff --git a/net/minecraft/world/entity/player/Player.java b/net/minecraft/world/entity/player/Player.java
index 7b15e5dcc27876e70ac54b2f45d577b670bee448..d431454e998fb8d53ec3b8d49d5cb2c121c2c0b0 100644
--- a/net/minecraft/world/entity/player/Player.java
+++ b/net/minecraft/world/entity/player/Player.java
@@ -1047,15 +1047,15 @@ public abstract class Player extends Avatar implements ContainerUser {
             if (this.level() instanceof ServerLevel serverLevel
                 && fun.bm.mili.carpet.config.modules.GeneralCompatConfig.creativeOneHitKill
                 && this.isCreative()
-                && net.minecraft.world.entity.EntitySelector.NO_CREATIVE_OR_SPECTATOR.test(target)) {
+                && net.minecraft.world.entity.EntitySelector.NO_CREATIVE_OR_SPECTATOR.test(entity)) {
                 if (this.isShiftKeyDown()) {
-                    for (Entity entity : this.level().getEntities(this, target.getBoundingBox().inflate(2.0D, 0.5D, 2.0D), candidate ->
+                    for (Entity aoeEntity : this.level().getEntities(this, entity.getBoundingBox().inflate(2.0D, 0.5D, 2.0D), candidate ->
                         candidate.isAttackable() && net.minecraft.world.entity.EntitySelector.NO_CREATIVE_OR_SPECTATOR.test(candidate))) {
-                        this.creativeInstantKill(serverLevel, entity);
+                        this.creativeInstantKill(serverLevel, aoeEntity);
                     }
                     this.playServerSideSound(SoundEvents.PLAYER_ATTACK_SWEEP);
                 } else {
-                    this.creativeInstantKill(serverLevel, target);
+                    this.creativeInstantKill(serverLevel, entity);
                     this.playServerSideSound(SoundEvents.PLAYER_ATTACK_CRIT);
                 }
                 return;
diff --git a/net/minecraft/world/item/ShearsItem.java b/net/minecraft/world/item/ShearsItem.java
index b7f8b52fcb2600e211293b196dfba168c2a31bb3..fc0a63099a86d5fc722037408eb314cc4e0da3e2 100644
--- a/net/minecraft/world/item/ShearsItem.java
+++ b/net/minecraft/world/item/ShearsItem.java
@@ -81,7 +81,7 @@ public class ShearsItem extends Item {
             return InteractionResult.SUCCESS;
         } else {
             // Mili start - shears wrench
-            InteractionResult result = org.leavesmc.leaves.util.ShearsWrenchUtil.tryApplyRotate(context, blockState);
+            InteractionResult result = org.leavesmc.leaves.util.ShearsWrenchUtil.tryApplyRotate(context, state);
             return result == null ? super.useOn(context) : result;
             // Mili end - shears wrench
         }
diff --git a/net/minecraft/world/item/crafting/RecipeManager.java b/net/minecraft/world/item/crafting/RecipeManager.java
index e5362305761966f616ac7fabef583d615430de7a..3c040e6688414c5b4b0ad93f04795a341375ba04 100644
--- a/net/minecraft/world/item/crafting/RecipeManager.java
+++ b/net/minecraft/world/item/crafting/RecipeManager.java
@@ -108,14 +108,26 @@ public class RecipeManager extends SimplePreparableReloadListener<RecipeMap> imp
         }
     }
 
+    /**
+     * Re-inject compat recipes into the current recipe map after a config reload.
+     * Called by CarpetCompatSync when carpet compat config is reloaded.
+     */
+    public void reinjectCompatRecipes() {
+        this.removeRecipe(BETTER_CRAFTABLE_BONE_BLOCK_RECIPE);
+        this.removeRecipe(BETTER_CRAFTABLE_DISPENSER_SHAPED_RECIPE);
+        this.removeRecipe(BETTER_CRAFTABLE_DISPENSER_SHAPELESS_RECIPE);
+        this.injectCompatRecipes(this.recipes);
+        this.finalizeRecipeLoading();
+    }
+
     private static RecipeHolder<?> createBetterCraftableBoneBlockRecipe() {
         return new RecipeHolder<>(
             BETTER_CRAFTABLE_BONE_BLOCK_RECIPE,
             new ShapedRecipe(
-                "carpet_ams_better_bone_block",
-                CraftingBookCategory.BUILDING,
+                new Recipe.CommonInfo(true),
+                new CraftingRecipe.CraftingBookInfo(CraftingBookCategory.BUILDING, "carpet_ams_better_bone_block"),
                 ShapedRecipePattern.of(Map.of('#', Ingredient.of(net.minecraft.world.item.Items.BONE)), "###", "###", "###"),
-                new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.BONE_BLOCK, 3)
+                net.minecraft.world.item.ItemStackTemplate.fromStack(new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.BONE_BLOCK, 3))
             )
         );
     }
@@ -124,15 +136,15 @@ public class RecipeManager extends SimplePreparableReloadListener<RecipeMap> imp
         return new RecipeHolder<>(
             BETTER_CRAFTABLE_DISPENSER_SHAPED_RECIPE,
             new ShapedRecipe(
-                "carpet_ams_better_dispenser",
-                CraftingBookCategory.REDSTONE,
+                new Recipe.CommonInfo(true),
+                new CraftingRecipe.CraftingBookInfo(CraftingBookCategory.REDSTONE, "carpet_ams_better_dispenser"),
                 ShapedRecipePattern.of(
                     Map.of('S', Ingredient.of(net.minecraft.world.item.Items.STICK), 'D', Ingredient.of(net.minecraft.world.item.Items.DROPPER), 'X', Ingredient.of(net.minecraft.world.item.Items.STRING)),
                     " SX",
                     "SDX",
                     " SX"
                 ),
-                new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.DISPENSER)
+                net.minecraft.world.item.ItemStackTemplate.fromStack(new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.DISPENSER))
             )
         );
     }
@@ -141,9 +153,9 @@ public class RecipeManager extends SimplePreparableReloadListener<RecipeMap> imp
         return new RecipeHolder<>(
             BETTER_CRAFTABLE_DISPENSER_SHAPELESS_RECIPE,
             new ShapelessRecipe(
-                "carpet_ams_better_dispenser",
-                CraftingBookCategory.REDSTONE,
-                new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.DISPENSER),
+                new Recipe.CommonInfo(true),
+                new CraftingRecipe.CraftingBookInfo(CraftingBookCategory.REDSTONE, "carpet_ams_better_dispenser"),
+                net.minecraft.world.item.ItemStackTemplate.fromStack(new net.minecraft.world.item.ItemStack(net.minecraft.world.item.Items.DISPENSER)),
                 List.of(Ingredient.of(net.minecraft.world.item.Items.BOW), Ingredient.of(net.minecraft.world.item.Items.DROPPER))
             )
         );
diff --git a/net/minecraft/world/item/crafting/SmithingTransformRecipe.java b/net/minecraft/world/item/crafting/SmithingTransformRecipe.java
index 432bfa6991919a2803846838a1dfe3415ae668c2..89983e7da244434a5346b23b49c748349cd79e34 100644
--- a/net/minecraft/world/item/crafting/SmithingTransformRecipe.java
+++ b/net/minecraft/world/item/crafting/SmithingTransformRecipe.java
@@ -117,7 +117,7 @@ public class SmithingTransformRecipe extends SimpleSmithingRecipe {
 
     // Leaves start - REI
     public SlotDisplay getResult() {
-        return this.result.display();
+        return new SlotDisplay.ItemStackSlotDisplay(this.result);
     }
     // Leaves end - REI
 
diff --git a/net/minecraft/world/level/block/Block.java b/net/minecraft/world/level/block/Block.java
index 4134fa658f09a9dcbb674dfdd91d2ffdfda912ba..0f9dd7b3991f18f4d643428ab600b502936bfac7 100644
--- a/net/minecraft/world/level/block/Block.java
+++ b/net/minecraft/world/level/block/Block.java
@@ -722,6 +722,13 @@ public class Block extends BlockBehaviour implements ItemLike {
     }
     // CraftBukkit end
 
+    // Mili start - reset push reaction
+    @org.jetbrains.annotations.Nullable
+    public net.minecraft.world.level.material.PushReaction getResetPushReaction() {
+        return null;
+    }
+    // Mili end - reset push reaction
+
     private record ShapePairKey(VoxelShape first, VoxelShape second) {
         @Override
         public boolean equals(final Object o) {
diff --git a/net/minecraft/world/level/block/CarvedPumpkinBlock.java b/net/minecraft/world/level/block/CarvedPumpkinBlock.java
index 35e054d503969a7999ff7867bddb65f94596d800..8a13418ef33c1a29d1a04ca5c680dbcc9d9039da 100644
--- a/net/minecraft/world/level/block/CarvedPumpkinBlock.java
+++ b/net/minecraft/world/level/block/CarvedPumpkinBlock.java
@@ -176,7 +176,7 @@ public class CarvedPumpkinBlock extends HorizontalDirectionalBlock {
             return;
         }
 
-        net.minecraft.world.entity.monster.Shulker shulker = EntityType.SHULKER.create(level, EntitySpawnReason.TRIGGERED);
+        net.minecraft.world.entity.monster.Shulker shulker = EntityTypes.SHULKER.create(level, EntitySpawnReason.TRIGGERED);
         if (shulker == null) {
             return;
         }
diff --git a/net/minecraft/world/level/block/SignBlock.java b/net/minecraft/world/level/block/SignBlock.java
index 17927fe45d45793a5f9696adb2fbb059231fad39..694b7f6b0240fe79a078def3e445afa93a8a4ce9 100644
--- a/net/minecraft/world/level/block/SignBlock.java
+++ b/net/minecraft/world/level/block/SignBlock.java
@@ -121,7 +121,7 @@ public abstract class SignBlock extends BaseEntityBlock implements SimpleWaterlo
                     }
                     if (level.getBlockEntity(pos1) instanceof net.minecraft.world.level.block.entity.RandomizableContainerBlockEntity) {
                         BlockState state1 = level.getBlockState(pos1);
-                        return state1.useItemOn(stack, level, player, hand, hitResult.withPosition(pos1));
+                        return state1.useItemOn(itemStack, level, player, hand, hitResult.withPosition(pos1));
                     }
                     return InteractionResult.TRY_WITH_EMPTY_HAND;
                     // Mili end - signContainerPassthrough
diff --git a/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java b/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
index 9d14176da1cb9ba0439366363fc2384215420f20..534f258b7b2b2aae14d889c0494392e8e360c0df 100644
--- a/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
+++ b/net/minecraft/world/level/block/entity/AbstractFurnaceBlockEntity.java
@@ -395,7 +395,7 @@ public abstract class AbstractFurnaceBlockEntity extends BaseContainerBlockEntit
         ItemStack oldStack = this.items.get(slot);
         boolean same = !itemStack.isEmpty() && ItemStack.isSameItemSameComponents(oldStack, itemStack);
         this.items.set(slot, itemStack);
-        itemStack.limitSize(this.getMaxStackLeaves(itemStack)); // Mili - item over-stack util
+        itemStack.limitSize(org.leavesmc.leaves.util.ItemOverstackUtils.getItemStackMaxCount(itemStack)); // Mili - item over-stack util
         if (slot == 0 && !same && this.level instanceof ServerLevel serverLevel) {
             this.cookingTotalTime = getTotalCookTime(serverLevel, this, this.recipeType, this.cookSpeedMultiplier); // Paper - cook speed multiplier API
             this.cookingTimer = 0;
diff --git a/net/minecraft/world/level/chunk/storage/RegionFileStorage.java b/net/minecraft/world/level/chunk/storage/RegionFileStorage.java
index 49fb94848d2336e7c6a2d2c0fa45e9901e8b3005..0e208eac6b7aed80a2c2484d8bc86e1e86c8ae63 100644
--- a/net/minecraft/world/level/chunk/storage/RegionFileStorage.java
+++ b/net/minecraft/world/level/chunk/storage/RegionFileStorage.java
@@ -65,7 +65,7 @@ public class RegionFileStorage implements AutoCloseable, ca.spottedleaf.moonrise
         final String extensionName = fullNameSplit[fullNameSplit.length - 1];
 
         if (!regionFormat.getArgument().equalsIgnoreCase(extensionName)) {
-            net.minecraft.server.MinecraftServer.setFatalException(new RuntimeException("Invalid region file format: " + extensionName + " expected " + regionFormat.getArgument()));
+            LOGGER.error("Invalid region file format: {} expected {}", extensionName, regionFormat.getArgument()); // Mili - setFatalException removed in 26.2
             throw new IOException("Invalid region file format: " + extensionName + " expected " + regionFormat.getArgument());
         }
 
diff --git a/net/minecraft/world/level/levelgen/feature/CoralFeature.java b/net/minecraft/world/level/levelgen/feature/CoralFeature.java
index 0ab324d8435826878861547fe3309ace9c37abe8..6165bb35d2bb37178c0d0d3f284c501b9cbb7364 100644
--- a/net/minecraft/world/level/levelgen/feature/CoralFeature.java
+++ b/net/minecraft/world/level/levelgen/feature/CoralFeature.java
@@ -33,6 +33,12 @@ public abstract class CoralFeature extends Feature<NoneFeatureConfiguration> {
 
     protected abstract boolean placeFeature(final LevelAccessor level, final RandomSource random, final BlockPos origin, final BlockState state);
 
+    // Mili start - FertilizableCoral support (public bridge for protected placeFeature)
+    public boolean placeFeaturePublic(final LevelAccessor level, final RandomSource random, final BlockPos origin, final BlockState state) {
+        return this.placeFeature(level, random, origin, state);
+    }
+    // Mili end - FertilizableCoral support
+
     protected boolean placeCoralBlock(final LevelAccessor level, final RandomSource random, final BlockPos pos, final BlockState state) {
         BlockPos above = pos.above();
         BlockState targetBlockState = level.getBlockState(pos);
